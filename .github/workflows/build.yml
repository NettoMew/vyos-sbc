name: build-image

# 原生 arm64 runner（ubuntu-24.04-arm）构建 vyos-sbc 整盘镜像。
# 关键：arm64 runner 上 `docker run --platform linux/arm64` 与 chroot 均为原生执行，
# 不挂 qemu 仿真 —— 这正是相对本地 x86 的提速来源（lb build 不再被仿真拖慢）。
#
# 两道 actions/cache：
#   ① work/src       —— u-boot / rkbin / rtl8125 / aic8800 的克隆（rkbin 体积大，省下载）
#   ② work/kernel(+) —— 交叉编出的内核树 + deb + 输入指纹。内核片段/补丁未变时，
#                       kernel 阶段据指纹跳过重编（约省 10–15min），且这棵已编树给
#                       r8125/aic8800 的 out-of-tree 模块编译用（它们需 Module.symvers）。
# base ISO 不缓存：每次全新 lb build（VyOS rolling 包集会变，求新鲜与可复现）。

on:
  workflow_dispatch:        # 只手动触发（避免每次 push 自动跑、吃 runner 时间）
    inputs:
      board:
        description: "设备"
        type: choice
        options: [r5s, m28k, e20c, e52c, a5e]
        default: a5e
      kernel_mode:
        description: "内核构建方式（cross=宿主交叉,快;container=官方容器流程,正常但慢。带 out-of-tree 驱动的 m28k/r5s/e52c 只能 cross；e20c/a5e 两者皆可）"
        type: choice
        options: [cross, container]
        default: cross

concurrency:
  group: build-${{ github.ref }}-${{ github.event.inputs.board || 'a5e' }}
  cancel-in-progress: true

env:
  BOARD: ${{ github.event.inputs.board || 'a5e' }}
  # cross（默认）：宿主交叉编译,leaner、快,且产出宿主侧内核树供 r8125/aic8800 编 out-of-tree 模块。
  # container（正常）：官方 build.py 在 arm64 容器里跑（原生无 qemu,但更重）;无宿主侧内核树,
  #                    故只适合无 out-of-tree 驱动的板（e20c）。
  KERNEL_BUILD_MODE: ${{ github.event.inputs.kernel_mode || 'cross' }}
  # 官方 vyos/vyos-build:current 是单架构(amd64)、没有 arm64 变体 → 在 arm64 runner 上
  # 拉它只会得到 amd64（脚本会判出不对再回退本地构建，白拉一趟）。故直接 0：跳过 pull，
  # 用 work/vyos-build/docker 的 Dockerfile 在 arm64 上原生构建 builder 容器。
  BUILDER_PULL: "0"

jobs:
  build:
    runs-on: ubuntu-24.04-arm   # ← 原生 arm64，无 qemu
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # container（正常）模式没有宿主侧内核树,带 out-of-tree 驱动的板会在 aic8800/r8125
      # 阶段失败 → 早退并给清楚提示,省得白跑。
      - name: Validate kernel mode vs board
        run: |
          case "$BOARD" in
            e20c|a5e) ;;   # 纯主线、无 out-of-tree 驱动：container 也可
            *)
              if [ "$KERNEL_BUILD_MODE" = "container" ]; then
                echo "::error::container（正常）模式不支持 $BOARD —— 它带 out-of-tree 驱动（m28k=aic8800 / r5s,e52c=r8125）,需要宿主侧内核树,请改用 cross。当前只有 e20c/a5e 可走 container。"
                exit 1
              fi ;;
          esac

      # GHA runner 根盘默认空闲偏小（~14GB），本构建峰值约需 25–40GB。
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main   # 可按需 pin 到具体 commit
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: false   # 我们要 docker，别清运行时
          swap-storage: false

      - name: Install host build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            git curl rsync zstd xz-utils tar ca-certificates \
            parted dosfstools e2fsprogs squashfs-tools kmod util-linux \
            build-essential bc bison flex libssl-dev libelf-dev swig \
            gcc-aarch64-linux-gnu libc6-dev-arm64-cross libgnutls28-dev \
            python3 python3-dev python3-setuptools python3-pycryptodome python3-pyelftools xorriso \
            debhelper dpkg-dev fakeroot shellcheck
          # 交叉工具链兜底：若该平台未提供 aarch64-linux-gnu-* 前缀，软链到原生工具
          if ! command -v aarch64-linux-gnu-gcc >/dev/null; then
            for t in gcc ld objcopy objdump nm ar strip; do
              src=$(command -v "$t") && sudo ln -sf "$src" "/usr/local/bin/aarch64-linux-gnu-$t"
            done
          fi

      - name: Build regression tests
        run: |
          for test in tests/*.sh; do bash "$test"; done
          python3 tests/a5e-hwid.py
          python3 tests/a5e-pcie.py
          sudo python3 tests/boot-media.py
          python3 tests/net-tune.py
          for script in lib/*.sh scripts/*.sh tests/*.sh; do bash -n "$script"; done
          shellcheck -S warning -x lib/{sources,kernel,iso,uboot,builder,r8125,aic8800,deps,overlay,image,imgiso}.sh scripts/docker-build.sh tests/*.sh

      # ── 缓存①：源码克隆（按 build.conf 的 ref；rkbin 大，主要省下载/clone）─────
      - name: Cache sources (work/src)
        uses: actions/cache@v4
        with:
          path: work/src
          key: src-${{ runner.arch }}-${{ hashFiles('build.conf', 'lib/sources.sh', 'lib/r8125.sh', 'lib/aic8800.sh') }}
          restore-keys: |
            src-${{ runner.arch }}-

      # ── 缓存②：内核树 + deb + 输入指纹（片段/补丁未变即命中→跳过重编）──────────
      # work/kernel 含交叉编树（Module.symvers/signing_key，供 r8125）与产出的 deb；
      # kernel-inputs.sha256 是 build.sh 的内核输入指纹，命中后 kernel 阶段据此跳过。
      - name: Cache kernel tree + deb
        uses: actions/cache@v4
        with:
          path: |
            work/kernel
            work/state/kernel-inputs.sha256
          key: kernel-${{ runner.arch }}-${{ hashFiles('build.conf', 'lib/kernel.sh', 'lib/builder.sh', 'overlay/scripts/package-build/linux-kernel/config/**', 'overlay/scripts/package-build/linux-kernel/patches/kernel/**', 'boards/*/overlay/scripts/package-build/linux-kernel/patches/kernel/**') }}

      # 阶段拆两段，中间把缓存里的内核 deb 摆回 packages/ 让 kernel 阶段能据指纹跳过。
      # （work/vyos-build 由 sources 阶段 git clone，故 deb 不能预先放进它的 packages/，
      #   否则 clone 会因目录非空失败 —— 所以等 clone 完再拷。跳过 deps：其 qemu-binfmt
      #   检查在原生 arm64 会误报 fatal。）
      # ── 缓存③：本地构建的 arm64 builder 容器镜像 ───────────────────────────────
      # 官方 vyos/vyos-build:current 单架构(amd64)，arm64 上 BUILDER_PULL=0 走本地构建，
      # 每次约几分钟。docker save|zstd 进 cache，命中即 load，stage_builder 见镜像在就跳过。
      - name: Cache vyos-build arm64 container image
        id: builder-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/vyos-builder
          key: builder-${{ runner.arch }}-${{ hashFiles('build.conf', 'lib/builder.sh') }}

      - name: Load cached builder image
        if: steps.builder-cache.outputs.cache-hit == 'true'
        run: zstd -dc ~/.cache/vyos-builder/img.tar.zst | docker load

      - name: Sources + overlay + builder
        run: scripts/build.sh "$BOARD" --stages sources,overlay,builder

      - name: Save builder image to cache (on miss)
        if: steps.builder-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/.cache/vyos-builder
          docker save vyos-sbc/builder:rolling | zstd -T0 -19 > ~/.cache/vyos-builder/img.tar.zst

      - name: Restore cached kernel deb into packages/
        run: |
          shopt -s nullglob
          debs=(work/kernel/linux-image-*-vyos_*_arm64.deb)
          if (( ${#debs[@]} )); then
            mkdir -p work/vyos-build/packages
            cp -v "${debs[@]}" work/vyos-build/packages/
            echo "命中内核缓存 → deb 已就位，kernel 阶段将跳过重编"
          else
            echo "无内核缓存，kernel 阶段将交叉编译"
          fi

      - name: Kernel + ISO + board assets + U-Boot + image
        run: scripts/build.sh "$BOARD" --stages kernel,iso,aic8800,r8125,oled,sources,uboot,image,imgiso

      - name: Verify A5E PCIe kernel and firmware DT contract
        if: env.BOARD == 'a5e'
        run: |
          tmp=$(mktemp -d)
          trap 'rm -rf "$tmp"' EXIT
          dpkg-deb -x work/vyos-build/packages/linux-image-*-vyos_*_arm64.deb "$tmp"
          python3 tests/a5e-pcie.py \
            --kernel-config "$tmp"/boot/config-*-vyos \
            --kernel-dtb "$tmp"/usr/lib/linux-image-*-vyos/allwinner/sun55i-a527-cubie-a5e.dtb \
            --uboot-config work/src/u-boot/.config \
            --uboot-dtb work/src/u-boot/u-boot.dtb

      - name: Upload image
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: vyos-${{ env.BOARD }}-image
          path: |
            out/*.img.xz
            out/*.iso
            out/*.sha256
            out/*.json
          if-no-files-found: error
          retention-days: 14

      # 失败时把构建日志/部分产物捞出来便于排查
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ env.BOARD }}
          path: |
            work/*.log
            out/
          if-no-files-found: ignore
          retention-days: 7
