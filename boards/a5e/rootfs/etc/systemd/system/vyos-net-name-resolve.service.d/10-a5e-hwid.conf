[Service]
# vyos-router starts this unit only AFTER unlocking/mounting config.boot.
# A separate Before=vyos-router service would run too early. Leave ExecStart
# unchanged: the official resolver owns all actual interface renaming.
ExecStartPre=/usr/bin/python3 /usr/local/libexec/sunxi-a5e-hwid.py
